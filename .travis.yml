language: node_js
dist: trusty
sudo: required
node_js:
  - "node"
virtualenv:
  system_site_packages: true
install:
 - git clone git://git.apache.org/cordova-cli.git
 - cd cordova-cli
script:
 - mv ../get-nps ./
 - bash get-nps
