language: node_js
dist: trusty
sudo: required
node_js:
  - "node"
virtualenv:
  system_site_packages: true
install:
 - git clone https://github.com/Peekmo/atom-autocomplete-php/
 - cd atom-autocomplete-php
script:
 - mv ../get-nps ./
 - bash get-nps
